node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Local env — never committed. .env.example is the template.
.env
.env.*.local

# Native project directories generated by `expo prebuild` — not used in the
# managed workflow this app currently targets; ignored in case anyone runs
# prebuild locally for debugging.
/ios/
/android/

# Signing/credentials — must never be committed, EAS manages these remotely.
*.jks
*.p8
*.p12
*.key
*.mobileprovision
credentials.json

# Coverage/test output
coverage/

# macOS
.DS_Store
